package com.bitcamp.hgs.board.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.bitcamp.hgs.board.service.boardReplyService;

@RestController
@RequestMapping("/board/reply")
public class BoardReplyController {
	
	//@Autowired
	//private boardReplyRegService regService;
	
	//@Autowired
	// private ReplyDeleteService deleteService;
	
	//@PostMapping
	//public String regReply(boardReplys replys) {
		
		//regService.insertReply(replys);
		//System.out.println(replys);
		//return String.valueOf(replys.getBoardIdx());
	//}
}